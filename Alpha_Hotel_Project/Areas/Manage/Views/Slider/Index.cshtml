@model HomeViewModel
@{
    int count = 0;
}

<div class="container-fluid pt-4 px-4">
    <div class="col-12">
        <div class="bg-light rounded h-100 p-4">
            <h6 style="margin-bottom: 0.5rem !important;" class="mb-4 border-bottom">Slider List</h6>
            <a style="float:right" class="btn btn-danger m-2" asp-action="create" asp-controller="slider">Create Slider</a>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Image</th>
                        <th scope="col">Title</th>
                        <th scope="col">Button Text</th>
                        <th scope="col">Actions</th>
                    </tr>
                </thead>
                <tbody style="vertical-align: middle;">
                    @foreach (var item in Model.Sliders)
                    {
                        count++;
                        <tr>
                            <th scope="row">@count</th>
                            <td>
                                <img style="width: 50px;height:50px;border-radius: 50%;" src="~/uploads/slider/@item.Image">
                            </td>
                            <td>@item.Title</td>
                            <td>@item.ButtonText</td>
                            <td>
                                <a class="btn btn-sm btn-sm-square btn-outline-success m-2" asp-action="update" asp-route-id="@item.Id" asp-controller="slider"> <i class="fa fa-pen"></i> </a>
                                <a class="btn btn-sm btn-sm-square btn-outline-danger m-2 delete-button" asp-route-id="@item.Id" asp-action="delete" asp-controller="slider"> <i class="fa fa-trash"></i> </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>